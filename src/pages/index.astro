---
// Página principal - Single Page con todas las secciones
import Layout from '../layouts/Layout.astro'
import Hero from '../components/Hero.astro'
import Invitation from '../components/Invitation.astro'
import Gallery from '../components/Gallery.astro'
import Family from '../components/Family.astro'
import Timeline from '../components/Timeline.astro'
import DressCode from '../components/DressCode.astro'
import Gifts from '../components/Gifts.astro'
import Location from '../components/Location.astro'
import Footer from '../components/Footer.astro'
import SplashScreen from '../components/SplashScreen.tsx'
import MusicPlayer from '../components/MusicPlayer.tsx'
import GuestBook from '../components/GuestBook.tsx'
import RandomWish from '../components/RandomWish.tsx'
import PhotoUpload from '../components/PhotoUpload.tsx'
---

<Layout title="XV Años Camila Isabella | 18 de Junio 2026">
	<!-- Splash de bienvenida + reproductor de música -->
	<SplashScreen client:load />
	<MusicPlayer client:load />

	<main>
		<Hero />

		<Invitation />

		<Gallery />

		<Family />

		<Timeline />

		<DressCode />

		<Gifts />

		<section id="guestbook" class="py-16 px-6 bg-white">
			<div class="reveal">
				<h2 class="font-serif text-3xl sm:text-4xl text-primary-800 italic text-center mb-2">
					Mensajes de Felicitación
				</h2>
				<p class="font-sans text-sm text-primary-600 text-center mb-10">
					Déjale tus mejores deseos a Camila
				</p>
			</div>
			<GuestBook client:visible />
			<RandomWish client:load />
		</section>

		<Location />

		<section id="confirm" class="py-16 px-6 bg-white">
			<div class="max-w-md mx-auto text-center reveal">
				<h2 class="font-serif text-3xl sm:text-4xl text-primary-800 italic mb-3">
					Confirma tu Asistencia
				</h2>
				<p class="font-sans text-sm text-primary-600 mb-8">
					Confirma tu asistencia para recibir tus pases de acceso
				</p>
				<a
					href="https://wa.me/526182615616?text=Hola%2C%20quiero%20confirmar%20mi%20asistencia%20a%20los%20XV%20de%20Camila%20Isabella"
					target="_blank"
					rel="noopener noreferrer"
					class="inline-block px-10 py-3 rounded-full font-display text-sm tracking-widest uppercase text-white transition-all duration-300 hover:scale-105 hover:shadow-lg"
					style="background: linear-gradient(135deg, #D4AF37, #B8860B);"
				>
					Confirmar
				</a>
			</div>
		</section>

		<!-- Sección de subir fotos: oculta hasta 24h antes del evento (17 jun 2026 19:00 CST) -->
		<section id="photos" class="py-16 px-6" style="display: none; background-color: #fbf5f2;" data-reveal-time="2026-06-17T19:00:00-06:00">
			<div class="reveal">
				<p class="font-display text-xs tracking-[0.35em] text-gold-500 uppercase text-center mb-2">
					Comparte tus momentos
				</p>
				<h2 class="font-serif text-3xl sm:text-4xl text-primary-800 italic text-center mb-3">
					Sube tus Fotos
				</h2>
				<p class="font-sans text-sm text-primary-600 text-center mb-10">
					Sube los momentos capturados durante la fiesta para que Camila pueda verlos
				</p>
			</div>
			<PhotoUpload client:visible />
		</section>

		<!-- Mostrar la sección de fotos 24h antes del evento -->
		<script is:inline>
			const photoSection = document.getElementById('photos')
			if (photoSection) {
				const target = new Date(photoSection.dataset.revealTime).getTime()
				if (Date.now() >= target) {
					photoSection.style.display = ''
				}
			}
		</script>

		<Footer />
	</main>
</Layout>
